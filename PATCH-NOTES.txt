V2.1.6 DEBUG
- Does not change Firebase project/key/VAPID.
- Shows the exact Firebase initialization error.
- Checks HTTPS secure context, Notification, Service Worker, PushManager,
  IndexedDB, Firebase module imports, and messaging isSupported().
- Use this only to diagnose why initFirebase fails before Push registration.
